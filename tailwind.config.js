module.exports = {
    darkMode: 'class', // This is the default; you don't need to change it
    content: ['./**/*.php', './**/*.js'], // Important so PHP and JS files are scanned
    theme: {
      extend: {},
    },
    plugins: [],
  };
  